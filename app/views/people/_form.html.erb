<%= simple_form_for([@org, @person]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :twitter %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
